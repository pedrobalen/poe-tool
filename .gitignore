# Claude Code local config, skills, and settings (not part of the project)
.claude/

# Local third-party data: the GGG passive tree export used by cmd/treegen.
# Download it from github.com/grindinggear/skilltree-export when needed.
/skilltree-export-*

# Binaries
*.exe
/poe-build-overlay
/dist/

# Test / build artifacts
*.out
*.test

# Local SQLite artifacts (should never be committed)
*.db
*.db-wal
*.db-shm
